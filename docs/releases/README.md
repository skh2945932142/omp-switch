# Release Notes

Each supported release must add one file named `vX.Y.Z.md` in this directory. The release workflow uses that file as the GitHub Release body and fails if it is missing.

Use these headings when they apply:

- Added
- Changed
- Security
- Fixed
- Known Limitations

Describe feature-flag status for gateway, OAuth integration, session indexing, and update checking explicitly.
